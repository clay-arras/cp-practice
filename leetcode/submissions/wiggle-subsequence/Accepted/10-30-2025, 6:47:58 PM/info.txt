{"id":1816432704,"question_id":376,"lang":"python3","lang_name":"Python3","time":"2 weeks, 4 days","timestamp":1761875278,"status":10,"status_display":"Accepted","runtime":"7 ms","url":"/submissions/detail/1816432704/","is_pending":"Not Pending","title":"Wiggle Subsequence","memory":"20.1 MB","code":"class Solution:    \n    def wiggleMaxLength(self, nums: List[int]) -> int:\n        def update_ans(ans, x):\n            return [_ for _ in x] if len(x) > len(ans) else ans\n\n        @cache\n        def solve_rec(idx: int, inc: bool) -> list[int]:\n            \"\"\"\n            dp[arr][inc] is the longest zigzag array you can form using arr and the CURR\n            zigzag should be inc / dec (i.e. the next value should be LESS than curr)\n            \"\"\"\n            curr = nums[idx]\n            ans = []\n\n            if idx == 0:\n                return [curr]\n\n            if nums[idx - 1] < curr and inc:\n                x = [*solve_rec(idx - 1, not inc), curr]\n                ans = update_ans(ans, x)\n            elif nums[idx - 1] > curr and not inc:\n                x = [*solve_rec(idx - 1, not inc), curr]\n                ans = update_ans(ans, x)\n            else:\n                x = solve_rec(idx - 1, inc)\n                ans = update_ans(ans, x)\n\n            return ans\n\n        x = solve_rec(len(nums)-1, True)\n        y = solve_rec(len(nums)-1, False)\n        return max(len(x), len(y))","compare_result":"11111111111111111111111111111111","title_slug":"wiggle-subsequence","has_notes":false,"flag_type":1,"frontend_id":1332}