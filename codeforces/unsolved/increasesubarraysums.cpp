#include <bits/stdc++.h>
using namespace std;

void solve(){
    int n, x; cin >> n >> x;
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
   	freopen("../io/input.txt", "r", stdin);

   	int t; cin >> t;
   	while(t--){
   		solve();
   	}
	return 0;
}
