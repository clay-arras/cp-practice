lets assume all element counts are the same (use map increment decrement trick)





if there is a duplicate, then the answer is always yes (we can stall infinitely)
else, it has to do with PARITY. they can't be one swap apart
so its just how many things it has in common with the sorted array
